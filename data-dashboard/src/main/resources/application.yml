spring:
  application:
    name: data-dashboard
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 9004                    # port of current service in Eureka

eureka:
  instance:
    hostname: data-dashboard         # host name of instance in Eureka
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/